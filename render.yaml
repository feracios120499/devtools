services:
  - type: web
    name: devtools
    env: node
    buildCommand: npm install -g @angular/cli && npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    autoDeploy: true 